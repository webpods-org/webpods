{
  "name": "webpods",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/cli",
    "packages/ssh",
    "packages/compose",
    "packages/docker",
    "packages/artifacts",
    "packages/commands"
  ],
  "scripts": {
    "build": "tsonic restore && npm run -w @webpods/ssh build && npm run -w @webpods/compose build && npm run -w @webpods/docker build && npm run -w @webpods/artifacts build && npm run -w @webpods/commands build && npm run -w webpods-cli build",
    "typecheck": "npm run build && npm run -w @webpods/ssh typecheck && npm run -w @webpods/compose typecheck && npm run -w @webpods/docker typecheck && npm run -w @webpods/artifacts typecheck && npm run -w @webpods/commands typecheck && npm run -w webpods-cli typecheck"
  },
  "devDependencies": {
    "@tsonic/core": "^10.0.1",
    "@tsonic/dotnet": "^10.0.1",
    "@tsonic/globals": "^10.0.1",
    "@tsonic/tsbindgen": "^0.7.26",
    "tsonic": "^0.0.29",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
