# WebPods Environment Variables
# Copy this file to .env and update with your values

# This file contains secrets and environment-specific overrides
# Defaults are built into the application - only set these if you need different values

# ===== OPTIONAL OVERRIDES =====
# Uncomment and modify only if you need different values than defaults

# Server (defaults: 0.0.0.0:3000, http://localhost:3000)
# HOST=0.0.0.0
# PORT=3000
# PUBLIC_URL=https://yourdomain.com

# Database (defaults: localhost:5432, webpodsdb, postgres user)
# WEBPODS_DB_HOST=localhost
# WEBPODS_DB_PORT=5432
# WEBPODS_DB_NAME=webpodsdb
# WEBPODS_DB_USER=postgres

# Logging (default: info)
# LOG_LEVEL=debug

# Test Mode Security (NEVER enable in production!)
# NODE_ENV=test                  # Required for test mode
# WEBPODS_TEST_MODE=enabled      # Required for test mode  
# Both must be set AND request must come from localhost for test headers to work

# CORS (default: *)
# CORS_ORIGIN=https://app.example.com,https://admin.example.com

# Max payload size (default: 10mb)
# MAX_PAYLOAD_SIZE=50mb

# ===== REQUIRED SECRETS =====

# Database password (required)
WEBPODS_DB_PASSWORD=your-database-password

# JWT and Session secrets (required for production)
JWT_SECRET=generate-a-random-256-bit-key-here
SESSION_SECRET=generate-another-random-256-bit-key-here

# OAuth Provider Secrets (required for each provider in config.json)
# The variable name must match what's referenced in config.json
GITHUB_OAUTH_SECRET=your-github-client-secret
GOOGLE_OAUTH_SECRET=your-google-client-secret
# Add other provider secrets as needed...