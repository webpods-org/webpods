{
  "name": "webpods-cli",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsonic build",
    "build:jit": "tsonic build --no-aot",
    "dev": "tsonic run --no-aot -- --help"
  },
  "devDependencies": {
    "@webpods/commands": "^0.0.0"
  }
}
