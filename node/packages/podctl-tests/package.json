{
  "name": "podctl-tests",
  "version": "0.0.2",
  "description": "Integration tests for podctl (WebPods CLI)",
  "type": "module",
  "scripts": {
    "test": "LOG_LEVEL=error mocha",
    "test:grep": "LOG_LEVEL=error mocha --grep",
    "test:watch": "LOG_LEVEL=error mocha --watch",
    "build": "tsc --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@types/chai": "^5.0.1",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.10.10",
    "chai": "^5.1.2",
    "jsonwebtoken": "^9.0.2",
    "mocha": "^11.0.1",
    "webpods-test-utils": "file:../webpods-test-utils"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.7",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
